from itertools import combinations
n = int(input())
letters = input().split()
k = int(input())
all_combos = list(combinations(letters, k))
favorable = [c for c in all_combos if 'a' in c]
print(len(favorable) / len(all_combos))
